DECLARE
CURSOR EMPNAME IS SELECT UPPER(EMPNAME)FROM EMPLOYEE;
EMPNAME VARCHAR2(30);
BEGIN
       OPEN EMPNAMES;
       LOOP
             FETCH EMPNAMES INTO EMPNAME;
             EXIT WHEN EMPNAMES%NOT FOUND;
             PLAINDROME_CHECKER(EMPNAME);
       END LOOP;
       CLOSE EMPNAMES;
END;
