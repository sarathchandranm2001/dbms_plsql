CREATE OR REPLACE PROCEDURE PALINDROME_CHECKER(ARG IN VARCHAR2) AS
REV VARCHAR(30);
BEGIN
     SELECT REVERSE(ARG)INTO REV FROM DUAL;
     IF ARG = REV 
     THEN
            DBMS_OUTPUT.PUT_LINE(ARG);
     END IF;
END;